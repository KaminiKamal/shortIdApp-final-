<!DOCTYPE html>
<html>
<meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.1.1/jquery.min.js"></script>
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>
  <head>
    <title></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>

 
  <div class="panel panel-default">
    <div class="panel-heading">
     <h4>Type the full url</h4>
     <input type="text" class="form-control"  id="oldurl" style="width:80%"/>
     <div style="float:right;margin-top:-35px">
    <button type="button" class="btn btn-primary" id="btn" style="width:200px">OK</button></div></div>
    <div class="panel-body" id="newid"></div>
  </div>


    
    
    
  <div ></div><html>
<script>
$(document).ready(function() {
    $('#btn').click(function () {
      var data={
      	'originalurl' : $("#oldurl").val()
      	
                    };
      console.log(data);
      	$.ajax({
        type:"POST",
        url:"/form",
        data :  data, 
        //dataType: 'json',  // multiple data sent using ajax
        success: function (result) {
           alert("shortid generated");
          var out = result.oldurl;
          var c = result.shorturl;
                $("#newid").html('<a href="http://'+out+'">'+c+'</a>');
           //window.location = '/display';
        }
        });
    });
});
</script>